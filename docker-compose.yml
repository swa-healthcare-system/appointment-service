services:
  postgres:
    restart: unless-stopped
    image: postgres:10
    ports:
      - "5555:5432"
    volumes:
      - "postgres-data:/var/lib/postgresql/data"
    environment:
      - "POSTGRES_PASSWORD=password"
volumes:
  postgres-data: